import DestinationWorkspaceSelection from '@/components/workspace/DestinationWorkspaceSelection';

// This page now delegates to DestinationWorkspaceSelection component
const WorkspaceSelection = () => {
  return <DestinationWorkspaceSelection />;
};

export default WorkspaceSelection;